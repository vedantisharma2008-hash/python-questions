# 1. what is dynamic typing?
# Dynamic typing in Python means you don’t declare variable types in advance — the type is decided at runtime based on the
#  value assigned, and it can change later. This makes Python flexible but also means type errors only show up when the
#  program runs.